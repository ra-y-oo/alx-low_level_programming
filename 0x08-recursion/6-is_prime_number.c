#include "main.h"
/**
 * is_prime_number - returns 1 if the input is a prime number otherwise returns 0
 * Return:int
 */
int is_prime_number(int n)
{
    if (n/1 == n, n/n == 1)
    {
        return 1;
    }else
    {
        return 0;
    }
}
